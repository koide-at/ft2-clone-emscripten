# ft2-clone Web スモークテスト・チェックリスト

このチェックリストは、以下でビルドしたあとに実施してください。

`FT2_WEB_PTHREADS=OFF ./make-web.sh`

サーバ起動は以下を使用してください。

`python3 serve_web.py --dir release/other`

## 1) 起動

- `http://127.0.0.1:8765/` または `http://127.0.0.1:8765/index.html` を開く
- メイン UI が表示される（ブラウザ背景色/黒画面のまま固まらない）
- 致命的なランタイムエラーダイアログが出ない

## 2) 再生

- 正常な XM モジュールを読み込める
- 再生開始後、音が出る
- 再生中にパターン位置/時間表示が更新される

## 3) 基本編集

- パターンエディタで最低 1 ノート入力できる
- 楽器/サンプル選択を変更できる
- 編集後も UI が固まらず操作できる

## 4) ファイル入出力（Web 動作）

- 画面上部ツールバーの「Import to Disk op.」で `.xm` 等を取り込み、**Disk op.** の一覧に表示される
- Disk op. からファイルを選んでモジュールを読み込める（現在フォルダは IDBFS の `/ft2_persistent` 相当）
- モジュール保存でブラウザのダウンロードが始まる
- WAV レンダリングでダウンロードが始まる
- サンプル/インストゥルメント保存でダウンロードが始まる

## 5) 永続化

- 設定（オーディオ/UI）を変更する
- ページを再読み込みする
- 設定が保持される（IDBFS パス: `/ft2_persistent`）

## 6) MIDI（任意）

- 必要時にブラウザの MIDI 許可ダイアログが出る
- MIDI 入力デバイス一覧を開いても固まらない
- （デバイスがある場合）MIDI ノート入力がアプリ側に反映される

## 7) 回帰確認

- タブを完全に閉じてから再度起動しても動作する
- 無限読み込み状態にならない
- 再生停止の繰り返しでクラッシュしない

## 現時点の既知制約

- 現在安定動作を確認している構成は `FT2_WEB_PTHREADS=OFF`。
- DevTools の ScriptProcessorNode 警告は既知で、現時点では致命的ではない。

## 8) キー再割当て（Web）

- 上部ツールバーの `Keybinds` を開く
- 任意のショートカット（例: `Show Disk Op`）を別キーへ割り当てて `Save`
- 新しいショートカットで機能が動作する
- 同一キー+同一修飾子の重複割当て時は、保存時に警告が表示される（保存自体は可能）
- `Reset Defaults` で既定値に戻る
- ページ再読み込み後も設定が保持される

## 9) フルスクリーン遷移（Web）

- `Alt+Enter` でフルスクリーンへ入る
- フルスクリーン時に表示が崩れない（キャンバス位置/サイズ/入力が正常）
- `Alt+Enter` で通常表示へ戻る
- 復帰後も表示が崩れない（位置ずれ・拡大率ずれ・入力ずれがない）